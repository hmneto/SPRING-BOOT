pipeline{
    agent { label 'built-in' } 
    tools {
        maven '3.8.4'
    }
    stages{
        stage('teste'){
            steps {
                sh 'mvn --version'
            }
        }
    }
}